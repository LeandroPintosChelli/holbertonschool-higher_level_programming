#!/usr/bin/node
// Script that prints two arguments passed to it,
// In the following format: “ is ”

const variable = 'is';
const ar = process.argv[2];
const av = process.argv[3];

console.log(ar, variable, av);
